//Find the greatest number between two number by use if & else
#include<stdio.h>
int main ()
{
    int x,y;
    scanf("%d%d",&x,&y);
    if(x>y)
    printf("X is greater");
    else
    printf("Y is greater");
    return 0;
}