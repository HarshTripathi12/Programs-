//Write a program to adding of float same number without using 3 and - and +.
#include<stdio.h>
int main ()
{
    float x,sum;
    scanf("%f",&x);
    sum=2*x;
    printf("Sum is %.2f",sum);
    return 0;
}